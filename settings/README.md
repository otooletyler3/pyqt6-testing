# PyQt6 Settings App

## What is this?

settings/ is a fake save/reset settings app that writes to a .json file.

It is saved to: `settings.json`

## Setup

Activate the virtual environment from the repo root:
```bash
source .venv/bin/activate
```

## How to run

```bash
cd settings
python src/main.py
```